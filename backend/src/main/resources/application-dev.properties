# MySQL (dev: local docker-compose)
spring.datasource.url=jdbc:mysql://localhost:3306/campus_marketplace
spring.datasource.username=appuser
spring.datasource.password=apppassword
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql=true

# Redis (dev: local docker-compose)
spring.data.redis.host=localhost
spring.data.redis.port=6379

# Cookie: not Secure in dev (HTTP)
app.cookie.secure=false

# Email: console output
app.email.mode=console

# Dev-only JWT secret. Override with JWT_SECRET env var if you want.
app.jwt.secret=${JWT_SECRET:niTtFdPpBBas5qzlNtdg21B+QeItX2IaN7kroCDYuuA=}
